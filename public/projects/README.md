# Project media

Drop screenshots, GIFs, and short videos of each project **working** here, one folder
per project slug:

- `web-ti402/` — seeded with the real mockup + a screenshot
- `ecosystem/` — add: dashboard screenshot, a GIF of the orchestrator/agents running, etc.
- `chatbot/` — add: a GIF of asking it a question, the TF-IDF output, etc.
- `cyberpix/` — add: a GIF of gameplay, the menu, a sprite sheet, etc.

Recommended: images/GIFs as `.png` / `.gif` / `.webp`, clips as `.mp4` (H.264) kept short
(< ~15s, < ~10 MB). After adding files, tell me the filenames (or edit the `media: [...]`
array on the project in `src/data/projects.ts`) and they'll show on the case study.
